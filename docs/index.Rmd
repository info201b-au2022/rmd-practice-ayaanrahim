```{r setup, include = FALSE}
source("analysis.R")
```

Within the data, we can see that the total number of COVID cases in the U.S was `r get_total_deaths()`, while the total number of COVID deaths was `r get_total_deaths()`.

```{r plot, echo == FALSE}
cases_plot
```
